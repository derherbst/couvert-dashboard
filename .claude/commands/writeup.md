---
description: Generate WRITEUP.md from git history + the current conversation (uses the writeup skill).
---

Read the skill instructions at `.claude/skills/writeup/SKILL.md` and follow them to generate `WRITEUP.md` at the repo root.

If `WRITEUP.md` already exists, show a brief diff summary of substantive changes before overwriting.
